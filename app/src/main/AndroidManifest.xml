<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.teambuilding.scavenger"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QRCodeScannerActivity" />

        <activity android:name=".CafeClueActivity" />
        <activity android:name=".ToddClueActivity" />
        <activity android:name=".StationeryClueActivity" />
        <activity android:name=".SamiaClueActivity" />
        <activity android:name=".CimCityClueActivity" />
        <activity android:name=".RebeccaClueActivity" />
        <activity android:name=".BrownBagActivity" />
        <activity android:name=".TraceyClueActivity" />
        <activity android:name=".TechBarActivity" />
        <activity android:name=".SuccessActivity" />
    </application>

</manifest>